'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; export default function Detail({params}:{params:{id:string}}){const [name,setName]=useState(''),[url,setUrl]=useState(''),[message,setMessage]=useState('');async function save(e:React.FormEvent){e.preventDefault();const {error}=await createClient().from('businesses').update({business_name:name,target_url:url,updated_at:new Date().toISOString()}).eq('id',params.id);setMessage(error?.message||'Saved')}return <main className="mx-auto max-w-3xl p-6"><a href="/dashboard" className="text-brand-600">← Dashboard</a><form onSubmit={save} className="mt-8 space-y-4 rounded-2xl bg-white p-8 shadow"><h1 className="text-3xl font-bold">Business settings</h1><input required placeholder="Business name" value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-lg border p-3"/><input required type="url" placeholder="Google review URL" value={url} onChange={e=>setUrl(e.target.value)} className="w-full rounded-lg border p-3"/><button className="rounded-lg bg-brand-600 px-5 py-3 font-semibold text-white">Save changes</button>{message&&<p className="text-brand-600">{message}</p>}</form></main>}
