'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; export default function Claim(){const [code,setCode]=useState(''),[name,setName]=useState(''),[url,setUrl]=useState(''),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const {error}=await createClient().rpc('activate_card',{p_card_code:code,p_business_name:name,p_target_url:url,p_category:''});setMessage(error?.message||'Card registered.')}return <main className="mx-auto max-w-xl p-6"><a href="/dashboard" className="text-brand-600">← Dashboard</a><form onSubmit={submit} className="mt-8 space-y-4 rounded-2xl bg-white p-8 shadow"><h1 className="text-3xl font-bold">Register another card</h1><input required placeholder="SCAN-XXXXX" value={code} onChange={e=>setCode(e.target.value.toUpperCase())} className="w-full rounded-lg border p-3"/><input required placeholder="Business name" value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-lg border p-3"/><input required type="url" placeholder="Google review URL" value={url} onChange={e=>setUrl(e.target.value)} className="w-full rounded-lg border p-3"/><button className="w-full rounded-lg bg-brand-600 p-3 text-white">Register card</button>{message&&<p className="text-brand-600">{message}</p>}</form></main>}
