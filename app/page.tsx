import { redirect } from 'next/navigation'; import { createClient } from '@/lib/supabase/server';
export default async function Home(){const {data:{user}}=await (await createClient()).auth.getUser(); if(user)redirect('/dashboard'); return <main className="min-h-screen bg-gradient-to-br from-brand-900 to-slate-950 text-white"><nav className="mx-auto flex max-w-6xl justify-between p-6"><b className="text-xl">TapReview</b><a className="rounded-lg bg-white px-4 py-2 font-semibold text-slate-900" href="/login">Sign in</a></nav><section className="mx-auto max-w-6xl px-6 py-28"><p className="mb-4 text-brand-500">NFC + QR reviews</p><h1 className="max-w-3xl text-5xl font-bold tracking-tight md:text-7xl">Turn every customer tap into a review.</h1><p className="mt-6 max-w-xl text-lg text-slate-300">Manage every location, card and tap from one secure dashboard.</p><a href="/register" className="mt-10 inline-block rounded-xl bg-brand-500 px-6 py-3 font-bold text-white">Get started free</a></section></main>}
